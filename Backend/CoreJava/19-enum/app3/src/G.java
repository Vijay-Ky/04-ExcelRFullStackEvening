enum G
{
	G()
	{
	}
}
//without having any constants also, we can have an explicit no-arg constructor. 
//inside an enum without declaring constants straight away developing methods not allowed.
//enum should have a minimum one constant to provide an attribute or a method