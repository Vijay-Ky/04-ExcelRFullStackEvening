sql query
------------
CREATE TABLE CANDIDATES (ID INT AUTO_INCREMENT PRIMARY KEY, FIRST_NAME VARCHAR(50), LAST_NAME VARCHAR(50), AGE INT, ADDRESS TEXT, GENDER ENUM('M', 'F'), C TINYINT(1), CPP TINYINT(1), JAVA TINYINT(1), ANGULAR TINYINT(1), EDUCATION ENUM('BE', 'MCA', 'MBA'));

SELECT * FROM CANDIDATES;

DELETE FROM CANDIDATES;